# Graceful Doppler to Local Env Fallback configuration (Pillar 1 Privacy Compliance)
if command -v doppler &> /dev/null; then
    eval "$(doppler env --format exports)"
else
    if [ -f .env ]; then
        export $(cat .env | xargs)
    fi
fi
